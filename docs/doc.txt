linguagem -> py

oq o sistema vai fazer: 
    O cliente:
        ->cria conta
        ->faz login
        ->escolhe restaurante
        ->adiciona comida no carrinho
        ->faz pedido
        ->vê histórico

    O sistema:
        ->recomenda restaurantes
        ->mostra mais vendidos
        ->gera ranking
        ->calcula combinações
        ->usa grafos

usando as funcionalidades:
    login:
        ->login fo cliente 
        ->autenticação
        ->sessão

    hash:
        ->senha do usuario

    criptografio:
        ->criptografa o cpf do cliente

    contagem combinatoria:
        ->combinação de pedidos
        ->pratos
        ->ranking de comidas
        ->ranking de restaurantes

    matriz:
        ->restaurantes com seu tipo de comida, localização e nota
            ex: loja: hamburgueria - tipo de comida: hamburguer - localização - na esquina - notas: 7.63/10

    rescorrencia: (pensei em usar no carrinho der compras)
        ->função recursiva percorre itens no carrinho
        ->soma valores
        ->calcula total
    
    grafos:
        relacionar:
            ->restaurantes semelhantes
            ->comidas 
            ->pedidos
                ex: pizza -> humburguer
                    humburguer -> sushi
                    sushi -> temakeria

main menu:
    ->login   
    ->cadastro

menu do usuario:
    ->Ver restaurantes
    ->Fazer pedido
    ->Ver carrinho
    ->Histórico
    ->Recomendações
    -ranking dos restaurantes
    ->Logout
      